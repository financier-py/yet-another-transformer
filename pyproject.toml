[project]
name = "mini-transformer"
version = "0.1.0"
description = "Just implementation of the Transformer architecture"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.5",
    "pandas>=3.0.3",
    "torch>=2.12.0",
]
